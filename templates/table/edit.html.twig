{% extends 'base.html.twig' %}

{% block body %}
<div class="container mt-5">
    <div class="card shadow p-4">
        <h2>Modifier la {{ table.nom }}</h2>
        
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <div class="mt-4">
                <button class="btn btn-warning">Mettre à jour</button>
                <a href="{{ path('app_table_index') }}" class="btn btn-secondary">Annuler</a>
            </div>
        {{ form_end(form) }}
    </div>
</div>
{% endblock %}